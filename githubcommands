usage: github <command> [<args>]

Extensions github commands are:
   clone <username> <repository>        Clone the  repository at http://github.com/<username>/<repository>
   clone <repository>                   Clone your repository at git@github.com/<username>/<repository>
   commit [options]                     Commit on Github.com with your signed-of-by
   info                                 Give informations contained in your .gitconfig
   page                                 Update your webpages : git push origin 'master:gh-pages' && git pull origin 'master:gh-pages'
   pubkey                               Give your public RSA public key
   pull                                 Git pull origin master
   push                                 Git push origin master
   tiny <remote> <branch>               Only for SMALL projects : git pull <remote> <branch> && git add -A && git commit -s && git push <remote> <branch>
   tiny                                 Only for SMALL projects : git pull origin master && git add -A && git commit -s && git push origin master
   update <remote> <branch>             Git pull <remote> <branch> && Git push <remote> <branch>
   update <remote> <branch>             Git pull <remote> <branch> && Git push <remote> <branch>
   update                               Git pull origin master && Git push origin master

See 'git help' for more information on a git command.
